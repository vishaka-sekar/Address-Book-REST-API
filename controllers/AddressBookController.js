var express = require('express');
var router = express.Router();
var contactList = require('../controllers/AddressBookController');

var elastic = require('../models/db');

router.get('/', function (req, res) {
  
       
});